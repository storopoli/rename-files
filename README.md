# Rename Files

This is a simple Rust script that uses several `std` modules to simply rename my PDF files to terminal friendly filenames.

It will remove/convert all special characters, whitespaces, hyphens etc. in files ending with `.pdf`.
